// MigrationScan CLI entry point.
//
// Phase 0 is foundation only: this is a placeholder so the tool builds and packs
// as a global dotnet tool. The real CLI surface (argument parsing with
// System.CommandLine, output formats, exit codes) lands in Phase 1 onward.

Console.WriteLine("migrationscan: not yet implemented (Phase 0 foundation build).");
return 0;
